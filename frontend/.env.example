# Local dev: leave unset, Vite's proxy in vite.config.js forwards /api to the backend.
# Production (Vercel): set this to your deployed Railway backend URL, e.g.
# VITE_API_BASE_URL=https://your-app.up.railway.app/api
VITE_API_BASE_URL=
